# jest-angular-forms

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build jest-angular-forms` to build the library.

## Running unit tests

Run `nx test jest-angular-forms` to execute the unit tests via [Jest](https://jestjs.io).
